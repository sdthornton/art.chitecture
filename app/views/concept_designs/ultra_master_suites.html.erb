<section>
  <div class="container my-5 pb-5">
    <div class="row">
      <div class="col-xl-6 col-lg-7 offset-lg-1">
        <h1 class="font-weight-bold display-3">Ultra Master Suites</h1>
      </div>
    </div>

    <div class="image-board my-5">
      <% (1..88).each do |index| %>
        <div class="image-board-item">
          <%=
            image_tag "concepts/ultramastersuites/small/ultramastersuites-#{index}.jpg",
              class: "image-board-img",
              data: { large_src: asset_path("concepts/ultramastersuites/large/ultramastersuites-#{index}.jpg") }
          %>
        </div>
      <% end %>
    </div>
  </div>
</section>

<%= render partial: "/shared/image_gallery_footer" %>
